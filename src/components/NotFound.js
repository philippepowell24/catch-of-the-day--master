import React from "react";

// when a single return is included then do not need to write it, just a set of brackets is enough
const NotFound = () => (
	<div>
		<h2>Not Found!!!</h2>
	</div>
);

export default NotFound;